// Extiende los matchers de Jest con @testing-library/jest-dom
// para acceder a expects como: toBeInTheDocument(), toHaveValue(), etc.
// react-scripts ejecuta este archivo automáticamente antes de cada test suite.
import '@testing-library/jest-dom';
